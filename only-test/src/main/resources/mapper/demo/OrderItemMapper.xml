<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wuyou.onlytest.mapper.demo.OrderItemMapper">

    <insert id="insertBatchXml">
        INSERT INTO demo_order_item(order_id, product_name, quantity, price, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.productName}, #{item.quantity}, #{item.price}, NOW())
        </foreach>
    </insert>

</mapper>
